<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user_mapper">
	<select id="findUserByLoginId" parameterType="User" resultType="User">
	SELECT * FROM MEMBER_INFO WHERE user_id = #{userId} AND user_password = #{userPassword} AND user_type = #{userType}
	</select>
</mapper>